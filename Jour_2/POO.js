// class EmployeeAvailable{
//     constructor (typeEmployee, IsAvaible)

// }

class employees {
    constructor (status, isAvailable, isNotAvailable){
        this.status = status
        this.isAvailable = isAvailable 
        this.isNotAvailable = isNotAvailable
        let status = [respondant, manager, director]
    }
   
}

// {respondant, manager, director}