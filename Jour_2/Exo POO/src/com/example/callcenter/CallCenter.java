package com.example.callcenter;

import java.sql.SQLOutput;
import java.util.List;

public class CallCenter {
    public static void handleMultipleCalls (List<Call> calls, List<Worker> workers) {
        //Mon algo :
        //Workers sort by hierarchy
        //for each call of calls where handled = false
        //if worker where hierarchy = Respondent is available
        //worker.handlecall.call

    }
}
