{
    function majority(arr){
        const map = new Map();
        for (const number of arr){
            if (map.has(number, map.get(number))+1);
        
        }else{
            map.set(number,1);
        }
    }
}